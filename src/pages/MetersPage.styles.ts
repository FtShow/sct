import styled from 'styled-components';

export const MetersPageWrapper = styled.div`
    display: inline-flex;
    align-items: start;
    width: 100%;
    flex-direction: column;
`
export const PaginationContainer = styled.div`
  display: flex;
  justify-content: flex-end;
  width: 100%;
`